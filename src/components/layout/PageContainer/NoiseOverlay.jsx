import React from 'react';

/**
 * NoiseOverlay renders the fixed SVG noise grain backdrop texture.
 */
export default function NoiseOverlay() {
  return <div className="noise-overlay" aria-hidden="true" />;
}
